;; the difference from the old one is that:
;; when the new version is applied,
;; one more environment is constructed for the lambda form let
;; this environment's father environ is the run-time environ of make-withdraw

;; illustrations and figures unavailable here
